# Configurações de Ambiente - Exemplo
# Copie este arquivo para .env e preencha com valores reais

# Ambiente (development ou production)
ENVIRONMENT=development

# Chave secreta para JWT (gere uma aleatória para produção)
SECRET_KEY=sua-chave-super-secreta-aqui-mude-em-producao

# Database (opcional - usa SQLite por padrão)
# DATABASE_URL=sqlite:///./torneio_xadrez.db
# Para PostgreSQL no Render:
# DATABASE_URL=postgresql://user:password@host/database

# Configurações opcionais
# DEBUG=True
# ALLOWED_HOSTS=localhost,127.0.0.1
